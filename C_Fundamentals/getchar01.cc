#include <iostream>
using namespace std;
int main()
{
    char c;
    int nl = 0;
    // cout << EOF << endl;
  /*  while ((c = getchar()) != EOF)
    {
        if (c == '\n')
            nl++;
    }*/
    cout << (int)'\n'<<"\t"<<(char)((int)'\n');
}